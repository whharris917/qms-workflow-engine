"""QMS Workflow Engine — core runtime and execution logic.

Subpackages:
    runtime/    — Workflow interpretation (schema, evaluator, actions, renderer)
    execution/  — Table execution engine (PlanEngine, criteria, audit, persistence)

Modules:
    builder     — Create Workflow meta-handler
    utils       — Shared display helpers
"""
